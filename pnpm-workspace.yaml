packages:
  - "."
  - "packages/*"
  - "apps/*"
allowBuilds:
  esbuild: true
  lefthook: true
minimumReleaseAgeExclude:
  # Transitive dep of @takazudo/zudo-doc (we no longer depend on it directly);
  # too recently published to clear the minimumReleaseAge policy without this allowance.
  - '@takazudo/zudo-doc-history-server@0.2.15'
  - '@takazudo/zudo-doc@0.2.15'
