---
title: Quality Gates
sidebar_position: 2
description: The local and CI checks that protect generated data, tokens, builds, links, and tests.
---

## Inner loop

Run focused checks while changing code:

```sh
pnpm check
pnpm test:unit
```

`pnpm check` typechecks the root host, `@zudo-sg/demo-ui`, and the demo app. `pnpm test:unit` runs Vitest unit tests.

For docs-only work, use the filtered docs checks:

```sh
pnpm --filter @zudo-sg/doc check
pnpm --filter @zudo-sg/doc build
node scripts/check-links.mjs --dist=doc/dist
```

## Generated-data drift

Generated files are checked in, but their source of truth lives elsewhere. Both checks below wrap the `zudo-sg` CLI shipped by the `@takazudo/zudo-sg` styleguide engine (`packages/styleguide`), not a root-owned script:

- `pnpm check:sg-registry` runs `zudo-sg gen-registry --check`, verifying the styleguide story registries generated from `packages/demo-ui/src/**/*.stories.tsx`.
- `pnpm check:token-manifest` runs `zudo-sg gen-token-manifest --check`, verifying the UI token manifest generated from `packages/demo-ui/styles/tokens.css` and `packages/demo-ui/styles/colors.css`.

Both commands read the host's `zudo-sg.config.mjs` for their inputs and outputs (components roots, category order, token CSS files); see [Component Catalog Pipeline](../architecture/component-catalog-pipeline.mdx) and [Design Tokens](../architecture/design-tokens.mdx).

The docs workspace uses zudo-doc's package-owned default z-index tiers. It has no local z-index generator or drift check.

When a source file changes, run the matching generator before committing:

```sh
pnpm gen:sg-registry
pnpm gen:token-manifest
```

## Token and accessibility gates

`pnpm lint:tokens` enforces the tight-token strategy so components keep using approved semantic token utilities. `pnpm contrast:audit` checks configured color relationships for contrast regressions.

Story files are also covered by unit tests in `packages/demo-ui/src/stories/__tests__`, including module-shape and source-drift checks.

## Pre-push gate

The local pre-push command is:

```sh
pnpm b4push
```

It runs formatting checks for MDX, token lint, codegen drift checks, typecheck, unit tests, root build, demo build, link checks, HTML validation, Playwright smoke tests, and a final manual interactive smoke prompt.

Use the full gate before pushing broad changes. For documentation-only changes, the required focused checks are usually the docs build, docs check, link check against `doc/dist`, and `git diff --check`.

## Engine foreign-install gate

`pnpm verify:styleguide-install` proves the `@takazudo/zudo-sg` engine installs the same way a project outside this workspace would install it — not only through the workspace link the root host uses day to day:

```sh
pnpm verify:styleguide-install
```

It packs `packages/styleguide` with `pnpm pack`, installs the tarball plus exact `@takazudo/zfb`/`@takazudo/zudo-doc` pins into a copy of `fixtures/engine-host/` placed **outside** this repository (so its realpath resolves under `node_modules`, with no workspace link and no `packages: []` escape hatch), runs `zudo-sg gen-registry`, builds, and asserts the four injected routes plus the standalone preview stylesheet exist under the fixture's non-root `base` (`/styleguide/`). It then boots `zfb dev` once and asserts the dev-hydration seed puts `ConfiguredPreviewApp` into the built islands bundle — `zfb dev` only scans a host's own `pages/` for islands, so the fixture's `pages/lib/_zudo-sg-islands.ts` shim is exercised the same way the root host's is.

Run this gate after any change under `packages/styleguide` that touches the routes plugin, the preview-CSS plugin, staging, or the CLI; it is not part of the inner loop or `pnpm check`.

## CI gate

Pull requests target `main` or `base/**` branches. CI mirrors the b4push shape with separate jobs for token lint, codegen drift, typecheck, unit tests, root build, demo build, smoke E2E, and dist checks.

Scheduled rich CI is intentionally deferred until after public release. Visual and platform-specific checks are run ad hoc instead of as standing nightly infrastructure.
